import 'package:flutter/material.dart';

const Color bgColor = Color(0xFF1D1C1F);
const Color secondaryColor = Color.fromARGB(38, 123, 122, 122);
const Color textColor = Colors.white;
const Color buttonColor = Color(0xFFF1992D);
const Color borderColor = Color(0xFFF1992D);

// to use it in your page import the file
// use roboto fonts for the app , it doesn't need to be specified in the themes